# WEX Artifactory — set token locally (never commit real values):
# export ARTIFACTORY_NPM_TOKEN=... && npm install
# Use the public registry for unscoped packages; scoped @wexinc-healthbenefits uses Artifactory below.
registry=https://registry.npmjs.org/
@wexinc-healthbenefits:registry=https://usartifactorywexinc.jfrog.io/artifactory/api/npm/benefits-npm/
//usartifactorywexinc.jfrog.io/artifactory/api/npm/benefits-npm/:_authToken=${ARTIFACTORY_NPM_TOKEN}
